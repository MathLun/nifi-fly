# fly.toml app configuration file generated for nifi-fly-quiet-violet-1695 on 2025-07-26T07:28:26-03:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'nifi-fly-quiet-violet-1695'
primary_region = 'gru'

[build]
  dockerfile = 'Dockerfile'

[env]
  NIFI_WEB_HTTP_HOST = '0.0.0.0'
  NIFI_WEB_HTTP_PORT = '8080'

[[mounts]]
  source = 'nifi_data'
  destination = '/data'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  size = 'shared-cpu-4x'
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 2
